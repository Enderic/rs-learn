/*
    We write tests to make sure our logic is correct, which is something Rust can't do
    we use cargo new adder --lib to add a library named 'adder'
*/

fn main() {
    println!("Hello, world!");
}
